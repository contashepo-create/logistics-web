# خطة الانتقال الكاملة — من تطبيق سطح المكتب إلى تطبيق ويب

**المشروع المصدر:** `contashepo-create/logistic` — "برنامج الخدمات اللوجستيه"
**المشروع الهدف:** `contashepo-create/logistics-web`
**التاريخ:** 2026-08-31

---

## 1. ملخص تنفيذي

فحصتُ المشروع المصدر سطراً بسطر (٩٬٠٥٦ سطراً موزعة على ٢٩ ملفاً). النتيجة:

- المشروع المصدر هو **تطبيق سطح مكتب** مبني بـ **Python + PySide6 (Qt) + SQLite**، بواجهة عربية RTL كاملة.
- هو نظام محاسبي **بسيط بالفعل**: لا توجد شجرة حسابات (Chart of Accounts) ولا قيود يومية يدوية. المحاسبة كلها عبارة عن **فواتير نقل + سندات قبض + سندات دفع + رواتب**، والأرصدة تُحسب لحظياً من الحركات.
- لذلك مطلبك "نظام محاسبي بسيط بدون تعقيدات شجرة الحسابات والقيود" **مُحقَّق تلقائياً** — سننقل المنطق كما هو دون إضافة أي تعقيد.

### قرار التقنية المستهدف (كما أكّدت)
| البند | الاختيار |
|---|---|
| الإطار | **Next.js 16 (App Router) + React 19 + TypeScript** |
| قاعدة البيانات | **Supabase (PostgreSQL)** |
| مصادقة/صلاحيات | Supabase Auth + RLS |
| الأنماط | Tailwind CSS (بنفس ثيم الألوان الأصلي `#1f4e79`) |
| حالة البيانات | TanStack Query (بدل الإشارات والتحديث اليدوي) |

---

## 2. تشريح المشروع المصدر (ما سننقله بالكامل)

### 2.1 البنية الفعلية للملفات

```
logistic/
├── main.py                     # نقطة الإقلاع + تهيئة القاعدة + الثيم RTL
├── requirements.txt            # PySide6 + openpyxl
├── app/
│   ├── __init__.py             # APP_TITLE + الإصدار
│   ├── core/
│   │   ├── db.py               # الاتصال + المخطط الكامل (15 جدولاً) + الإعدادات الافتراضية
│   │   ├── rules.py            # القواعد العامة (السنوات المالية + تحقق الأرقام)
│   │   ├── repo.py             # CRUD كل الكيانات + كل قواعد المنع المحاسبي
│   │   └── calc.py             # محرك الأرصدة والكشوف والتقارير ولقطات الإغلاق
│   ├── utils/
│   │   ├── fmt.py              # الأرقام العربية، تنسيق المبالغ، الشهور، القواميس
│   │   ├── fonts.py            # اختيار خط عربي
│   │   └── exporter.py         # محرك Excel/PDF/طباعة الموحد
│   └── ui/
│       ├── theme.py            # الألوان + QSS
│       ├── widgets.py          # الجداول الذكية + الحقول + أشرطة التصدير + الإجماليات
│       ├── main_window.py      # التنقل الجانبي + مكدس الصفحات + فحص أول تشغيل
│       ├── dialogs_master.py   # عملاء/موظفون/سيارات/سنوات/خزائن/بنوك/لقطة الإغلاق
│       ├── dialogs_ops.py      # فاتورة النقل + القبض/الدفع + طباعة فاتورة العميل
│       ├── dialogs_payroll.py  # إصدار الراتب مع السلفيات
│       ├── statements.py       # كشوف الحساب المنفصلة (عميل/خزينة/بنك)
│       ├── pages_base.py       # صفحة CRUD المشتركة
│       ├── pages_master.py     # العملاء/الموظفون/السيارات/السنوات
│       ├── pages_ops.py        # الفواتير/القبض/الدفع
│       ├── pages_treasury.py   # الخزائن والبنوك
│       ├── pages_payroll.py    # الرواتب
│       ├── pages_reports.py    # التقارير الخمسة
│       └── page_settings.py    # الإعدادات
└── scripts/
    ├── seed_demo.py            # بيانات تجريبية
    ├── smoke_test.py / test_full.py / test_audit.py / test_ui_*.py / run_gui_check.py
```

### 2.2 قاعدة البيانات (15 جدولاً) — النقل إلى Supabase

| # | الجدول (SQLite) | الغرض | مكافئ Supabase (PostgreSQL) |
|---|---|---|---|
| 1 | `settings` | key/value للإعدادات (اسم الشركة، الهاتف، العملة، عبارة ZATCA) | جدول `settings` |
| 2 | `financial_years` | السنوات المالية (من/إلى، حالة open/closed) | جدول `financial_years` |
| 3 | `customers` | العملاء (كود CUST-####، رصيد افتتاحي) | جدول `customers` |
| 4 | `employees` | الموظفون (سائق/إداري، كود EMP-####) | جدول `employees` |
| 5 | `vehicles` | السيارات (لوحة، نوع، سائق افتراضي، كود VEH-####) | جدول `vehicles` |
| 6 | `cashboxes` | الخزائن (رصيد افتتاحي، كود CB-####) | جدول `cashboxes` |
| 7 | `banks` | البنوك (رقم حساب، IBAN، كود BNK-####) | جدول `banks` |
| 8 | `invoices` | رأس فاتورة النقل (رقم، تاريخ، عميل، ملاحظات، مرفقات) | جدول `invoices` |
| 9 | `invoice_trips` | نقلات الفاتورة (سيارة، سائق، من، إلى، سعر) | جدول `invoice_trips` |
| 10 | `trip_expenses` | مصروفات النقلة (trip/fuel/card/other) | جدول `trip_expenses` |
| 11 | `receipt_vouchers` | سندات القبض (cashbox/bank + customer/other) | جدول `receipt_vouchers` |
| 12 | `payment_vouchers` | سندات الدفع (trip/advance/vehicle/general) | جدول `payment_vouchers` |
| 13 | `payrolls` | الرواتب (أساسي، إضافات، خصم سلف، خصومات، صافي) | جدول `payrolls` |
| 14 | `advance_settlements` | تسويات السلف (ربط سلفة ↔ راتب) | جدول `advance_settlements` |
| 15 | `year_snapshots` | لقطات إغلاق السنوات (JSON) | جدول `year_snapshots` (عمود `jsonb`) |

> ملاحظة: README المصدر يقول "13 جدولاً" لكن المخطط الفعلي يحتوي **15 جدولاً** — سننقل الـ 15 جميعها.

### 2.3 المنطق المحاسبي (يجب نسخه حرفياً — كل معادلة)

#### أرصدة العملاء
```
رصيد العميل = الرصيد الافتتاحي + Σ أسعار نقلاته − Σ سندات قبض (نوع customer)
```
- `customer_balance(customer_id, before=None)` — مع دعم حساب الرصيد "قبل تاريخ" للكشوف.
- **إجمالي الفاتورة على العميل = مجموع أسعار النقلات فقط** (المصروفات داخلية لا تظهر للعميل).

#### أرصدة الخزائن والبنوك
```
الرصيد = الافتتاحي + Σ سندات قبض − Σ سندات دفع − Σ صافي الرواتب المنصرفة
```

#### إجماليات الفاتورة
```
trips_total      = Σ أسعار النقلات
expenses_total   = Σ مصروفات النقلات المباشرة
expected_profit  = trips_total − expenses_total        (الربح المتوقع)
later_payments   = Σ سندات دفع نوع "trip" على نقلاتها  (مصاريف لاحقة)
actual_profit    = expected_profit − later_payments     (الربح الفعلي)
customer_total   = trips_total                          (ما يُحمّل على العميل)
```

#### ربح النقلة
```
ربح النقلة = سعرها − مصروفاتها المباشرة − سندات الدفع اللاحقة عليها (خلال الفترة إن حُددت)
```

#### كشف حساب عميل
```
الافتتاحي + (الفواتير كمدين) − (سندات القبض كدائن) = الرصيد الجاري (يُحسب صفاً صفاً)
```

#### كشف حساب خزينة/بنك
```
الافتتاحي + (قبض وارد) − (دفع منصرف) − (رواتب منصرفة) = الرصيد الجاري
```

#### تقرير 1 — أرباح الفواتير والرحلات
```
لكل نقلة: الإيراد − المصروف المباشر − المصروف اللاحق = صافي الربح الفعلي
```

#### تقرير 3 — كشف موظف/سائق (ثلاثة تبويبات)
- **الرواتب:** الأساسي + الإضافات − خصم السلف − خصومات أخرى = الصافي.
- **السلف:** قيمة السلفة − المسدد = المتبقي + تفاصيل كل تسوية (تاريخها وقيمتها).
- **بدلات التريب:** مصروفات نوع `trip` على نقلات يقودها هذا السائق.

#### تقرير 4 — أداء السيارات
```
صافي ربحية السيارة = إيرادات نقلاتها − مصروفات رحلاتها المباشرة − صيانتها (سندات نوع vehicle)
```

#### تقرير 5 — الأرباح والخسائر (P&L)
```
الإيرادات:
  + إيرادات النقلات (Σ أسعار النقلات في الفترة)
  + الإيرادات الأخرى (Σ سندات قبض نوع other)
المصروفات:
  + المصروفات المباشرة (trip/fuel/card/other)
  + الرواتب الصافية المنصرفة
  + السلفيات المسجلة (نوع advance)
  + الصيانة (نوع vehicle)
  + المصاريف العامة (نوع general)
صافي الربح = إجمالي الإيرادات − إجمالي المصروفات
```

#### صيغة الراتب (Payroll)
```
الصافي = الأساسي + الإضافات − خصم السلف − الخصومات الأخرى
```
- قيد: **مجموع خصومات السلف الموزعة = قيمة خصم السلف** (بفارق ≤ 0.01).
- قيد: لا يتجاوز خصم كل سلفة **المتبقي منها**.
- قيد: الصافي لا يجوز أن يكون سالباً.

#### لقطة إغلاق السنة (Snapshot)
```
أرصدة كل العملاء + أرصدة كل الخزائن والبنوك + P&L كامل عن نطاق السنة
```

---

## 3. القواعد العامة على مستوى النظام (يجب نسخها)

1. **قاعدة السنوات المالية:**
   - لا يمكن إضافة/تعديل/حذف أي حركة خارج نطاق سنة مالية **مفتوحة**.
   - عند التعديل: التاريخ القديم **والجديد** يجب أن يقعا داخل سنة مفتوحة.
2. **المعالجة التلقائية بأثر رجعي:** الأرصدة لا تُخزَّن؛ تُحسب لحظياً من الحركات.
3. **منع الحذف المرتبط بحركات:**
   - لا يُحذف عميل له فواتير أو سندات قبض.
   - لا يُحذف موظف له رواتب/سلف/نقلات.
   - لا يُحذف سيارة لها نقلات أو سندات صيانة.
   - لا تُحذف خزينة/بنك له حركات قبض/دفع/رواتب.
   - لا تُحذف فاتورة/نقلة مرتبطة بسندات دفع (مصروف رحلة) — تُحذف السندات أولاً.
   - لا تُعدَّل/تُحذف سلفة خُصمت في رواتب — تُحذف الرواتب أولاً.
   - حذف راتب يُعيد سلفه المخصومة إلى "غير مسددة" تلقائياً.
4. **منع التداخل بين السنوات:** نطاق سنة لا يتداخل مع سنة أخرى (يمنع الاحتساب المزدوج).
5. **تحقق الأرقام:**
   - كل مبلغ يُقرَّب لمنزلتين وبسقف `999,999,999,999`.
   - النصوص بسقف 5000 محرف (منع DoS).
   - رفض NaN/inf، وقبول الأرقام العربية/الفارسية وفواصل الآلاف.
6. **الأكواد التلقائية:** `CUST-0001`, `EMP-0001`, `VEH-0001`, `CB-0001`, `BNK-0001`، وأرقام المستندات `INV-00001`, `RV-00001`, `PV-00001`, `PAY-00001`.

---

## 4. الأقسام والشاشات (قائمة كاملة للتنفيذ)

### شريط التنقل (٦ مجموعات)
| المجموعة | الصفحات |
|---|---|
| 📁 البيانات الأساسية | العملاء، الموظفون والسائقون، السيارات، السنوات المالية |
| 🏦 الخزائن والبنوك | الخزائن، البنوك |
| 🔄 العمليات اليومية | فواتير النقل، سندات القبض، سندات الدفع |
| 💰 الرواتب | إدارة الرواتب |
| 📊 التقارير الذكية | أرباح الفواتير والرحلات، كشف حساب عميل، كشف حساب موظف/سائق، أداء السيارات، P&L |
| ⚙️ النظام | الإعدادات |

### الجداول وأعمدة كل صفحة (تُنقل كما هي)
| الصفحة | الأعمدة | الأزرار الإضافية |
|---|---|---|
| العملاء | الكود، الاسم، الهاتف، العنوان، الافتتاحي، الحالي | 📄 كشف حساب |
| الموظفون | الكود، الاسم، الجنسية، الهاتف، النوع، الملاحظات | — |
| السيارات | الكود، اللوحة، النوع، السائق الافتراضي | — |
| السنوات | السنة، من، إلى، الحالة، عدد الحركات، اللقطات | 🖼️ لقطة إغلاق، 🔐 إغلاق/فتح |
| الخزائن | الرقم، الاسم، تاريخ الإنشاء، الافتتاحي، الحالي | 📄 كشف حساب |
| البنوك | الرقم، الاسم، التاريخ، رقم الحساب، IBAN، الافتتاحي، الحالي | 📄 كشف حساب |
| الفواتير | رقم، تاريخ، عميل، عدد النقلات، إجمالي النقلات، المصروفات المباشرة، الربح المتوقع، مصاريف لاحقة، الربح الفعلي | 🖨️ طباعة، 📄 PDF |
| سندات القبض | رقم، تاريخ، نوع، العميل/المصدر، أودع في، المبلغ، البيان | — |
| سندات الدفع | رقم، تاريخ، نوع، التوجيه، صرف من، المبلغ، البيان | — |
| الرواتب | رقم، تاريخ، موظف، نوع، عن شهر، طريقة الصرف، الأساسي، الإضافات، خصم السلف، خصومات أخرى، الصافي | — |

### النوافذ (Dialogs)
1. **CustomerDialog** — اسم*، هاتف (نص يحفظ الأصفار)، عنوان، رصيد افتتاحي، ملاحظات، رصيد حالي تلقائي.
2. **EmployeeDialog** — اسم*، جنسية، هاتف، نوع (سائق/إداري)*، ملاحظات.
3. **VehicleDialog** — لوحة*، نوع، سائق افتراضي (سائق فقط)، ملاحظات.
4. **YearDialog** — سنة*، من*، إلى*، الحالة، ملاحظات.
5. **AccountDialog** — اسم*، تاريخ إنشاء*، (بنك: رقم حساب + IBAN)، رصيد افتتاحي، ملاحظات.
6. **InvoiceDialog** — رأس (تاريخ*، عميل*، ملاحظات) + نقلات + مصروفات + مرفقات + إجماليات.
   - **TripDialog** — سيارة، سائق، من*، إلى*، سعر*، ملاحظات.
   - **ExpenseDialog** — نوع (تريب/بنزين/كارتة/أخرى)، مبلغ*، بيان.
   - **TripExpensesDialog** — إدارة مصروفات النقلة.
7. **ReceiptDialog** — تاريخ*، جهة الإيداع*، نوع (تحصيل من عميل/إيرادات أخرى)*، عميل (عند التحصيل)، مبلغ*، بيان.
8. **PaymentDialog** — تاريخ*، جهة الصرف*، نوع*، ثم **توجيه متغير حسب النوع**:
   - `trip` → اختيار الرحلة (قائمة النقلات بكل بياناتها).
   - `advance` → اختيار الموظف.
   - `vehicle` → اختيار السيارة + نوع المصروف (صيانة/كاوتش/أخرى).
   - `general` → بيان (إيجار/كهرباء...).
9. **PayrollDialog** — موظف*، شهر/سنة*، جهة الصرف*، أساسي*، إضافات+بيان، خصومات أخرى، **جدول السلفيات المتاحة تلقائياً مع حقل "الخصم الآن" لكل سلفة**، صافي تلقائي.
10. **SnapshotDialog** — 3 تبويبات: أرصدة العملاء / الخزائن والبنوك / P&L السنة.
11. **StatementDialog (3 أنواع)** — كشف عميل / كشف خزينة / كشف بنك بفلاتر تاريخ.

### التقارير (كلها فلاتر تاريخ + Excel/PDF/طباعة)
| # | التقرير |
|---|---|
| 1 | أرباح الفواتير والرحلات |
| 2 | كشف حساب عميل |
| 3 | كشف حساب موظف/سائق (3 تبويبات: رواتب/سلف/بدلات تريب) |
| 4 | أداء السيارات |
| 5 | الأرباح والخسائر (P&L) |

### الإعدادات
- اسم الشركة، هاتف، عنوان، رمز العملة (افتراضي "ر.س")، عبارة أسفل الفواتير (افتراضي "فاتورة نقل غير خاضعة لضريبة القيمة المضافة (ZATCA)").
- **نسخة احتياطية** (في الويب تُستبدل بـ Supabase Backups / تصدير JSON/Excel).

### طباعة فاتورة العميل الرسمية
- تُظهر **النقلات فقط** (من، إلى، السيارة، السائق، السعر) — تُخفي المصروفات والربح تماماً.
- ترويسة الشركة + رقم/تاريخ/عميل/هاتف + الإجمالي المستحق + ملاحظات + عبارة ZATCA.

---

## 5. مصفوفة الانتقال (ملف ↔ ملف)

| المصدر (Python) | الهدف (Next.js) | ملاحظات |
|---|---|---|
| `app/core/db.py` | `supabase/schema.sql` + `src/lib/supabase/*` | 15 جدولاً + RLS |
| `app/core/rules.py` | `src/lib/rules.ts` | السنوات المالية + التحقق |
| `app/core/repo.py` | `src/lib/api/*` (server actions/route handlers) | كل دوال CRUD والمنع |
| `app/core/calc.py` | `src/lib/calc.ts` | كل المعادلات والكشوف والتقارير |
| `app/utils/fmt.py` | `src/lib/format.ts` | الأرقام/المبالغ/الشهور/القواميس |
| `app/utils/exporter.py` | `src/lib/exporter.ts` (ExcelJS + jsPDF + `window.print`) | Excel/PDF/طباعة |
| `app/ui/theme.py` | `tailwind.config` + `globals.css` | نفس الألوان `#1f4e79` |
| `app/ui/widgets.py` | `src/components/ui/*` (DataTable، AmountInput، ...) | جدول CRUD + حقول |
| `app/ui/main_window.py` | `src/components/layout/Sidebar.tsx` | تنقل جانبي RTL |
| `app/ui/dialogs_*.py` | `src/components/dialogs/*` | كل النوافذ |
| `app/ui/pages_*.py` | `src/app/(app)/*/page.tsx` | كل صفحة |
| `app/ui/statements.py` | `src/components/statements/*` | كشوف الحساب |
| `scripts/seed_demo.py` | `scripts/seed.ts` | بيانات تجريبية في Supabase |

---

## 6. خطة التنفيذ على مراحل

### المرحلة 0 — التأسيس (Scaffold)
- إنشاء Next.js 16 + TypeScript + Tailwind + Supabase.
- نسخ متغيرات البيئة (Supabase URL/keys).
- تثبيت الحزم: `@supabase/supabase-js`, `@tanstack/react-query`, `lucide-react`, `date-fns`, `zustand`, `exceljs`, `jspdf`, خط عربي (`@fontsource/ibm-plex-sans-arabic`).

### المرحلة 1 — قاعدة البيانات (Supabase)
- كتابة `schema.sql` يطابق الـ 15 جدولاً مع تحويل الأنواع (`REAL`→`numeric`, `TEXT`→`text`, JSON→`jsonb`).
- القيود (CHECK) و الفهارس كما في المصدر.
- تمكين **RLS** للعزل (multi-tenant إن طُلب) + سياسات.
- دوال توليد الأكواد (`CUST-0001`...) وأرقام المستندات المتسلسلة.

### المرحلة 2 — منطق الأعمال (نسخة حرفية)
- `rules.ts` (السنوات المالية + التحقق).
- `calc.ts` (كل المعادلات في القسم 2.3).
- `repo.ts` → استدعاءات Supabase لكل كيان مع كل قواعد المنع.

### المرحلة 3 — الواجهة (نقل الشاشات)
- الهيكل: Sidebar RTL + الصفحات الست عشرة + كل نافذة/حوار.
- مكوّن `DataTable` ذكي (عرض/تعديل/حذف + أزرار إضافية + بحث).
- مكوّن `TotalsBar` (بطاقات الإجماليات) و `ExportBar` (Excel/PDF/طباعة).
- نموذج الفاتورة المعقّد (رأس + نقلات ديناميكية + مصروفات + مرفقات) باستخدام الحالة المحلية ثم الحفظ كحركة واحدة.
- نموذج الراتب مع جدول السلفيات التفاعلي.

### المرحلة 4 — التقارير والكشوف
- التقارير الخمسة + 3 كشوف + لقطة الإغلاق، مع فلاتر التاريخ والتصدير.

### المرحلة 5 — الإعدادات والمصادقة والبيانات التجريبية
- صفحة الإعدادات + طباعة فاتورة العميل.
- تسجيل دخول (Supabase Auth) إن كان مطلوباً لوضع الاستخدام عبر الإنترنت.
- `seed.ts` ينقل بيانات `seed_demo.py` حرفياً.

### المرحلة 6 — الاختبار والتحقق
- إعادة بناء الاختبارات (32 + 555 + 138 + 74 + 42 فحصاً في المصدر) كاختبارات Vitest/Jest على `calc.ts`/`rules.ts`/`repo.ts`.
- فحص تكافؤ الأرقام: نفس بيانات seed يجب أن تعطي نفس الأرصدة والتقارير حرفياً.

---

## 7. ملاحظات ومخاطر

1. **تسلسل أرقام المستندات:** SQLite يستخدم `MAX(number)+1`. في Supabase نستخدم `SERIAL`/`SEQUENCE` أو دالة Postgres، مع الحفاظ على نفس التسمية (`INV-00005`).
2. **المرفقات:** في المصدر تُخزَّن كملفات محلية. في الويب نستخدم **Supabase Storage** ونحفظ المسارات في عمود `attachments`.
3. **النسخ الاحتياطي:** زر "نسخة احتياطية" المحلي يُستبدل بتصدير JSON/Excel أو Supabase Backups.
4. **الطباعة:** نستخدم `window.print()` مع CSS مخصص لطباعة (بديل `QPrinter`)، و`jsPDF` لتصدير PDF، و`exceljs` لـ Excel بنفس التنسيق (ترويسة + رأس ملون `#1f4e79` + RTL).
5. **العزل متعدد الشركات:** إن كان النظام سيخدم أكثر من شركة، نضيف عمود `tenant_id` لكل الجداول مع سياسة RLS — حالياً المصدر أحادي الشركة.
6. **الدقة العشرية:** نستخدم `numeric(14,2)` في PostgreSQL بدل `REAL` لتجنب أخطاء الفاصلة العائمة، مع نفس التقريب لمنزلتين.

---

## 8. خلاصة ما تم تأكيده معك

- ✅ المصدر: `contashepo-create/logistic` (Python/PySide6/SQLite).
- ✅ الهدف: Next.js + Supabase.
- ✅ المحاسبة: **بسيطة** (لا شجرة حسابات ولا قيود يدوية) — المشروع المصدر بسيط أصلاً، فسننقله مطابقاً.
- ✅ النقل: **مطابق تماماً** لكل قسم ودالة ومنطق وسطر، بالعربية RTL وبنفس الألوان.

**جاهز للبدء في التنفيذ عند إشارتك.**
