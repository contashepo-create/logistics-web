# تقرير المراجعة الشاملة — بعد تعدّد المستخدمين (v25)

تاريخ المراجعة: 2026-09-06 · النطاق: تعديلات v25 + مراجعة المشروع بالكامل
النتيجة: **4 مشكلات حقيقية اكتُشفت وأُصلحت**، لا أخطاء محاسبية، لا ثغرات مستغَلّة.

الفحوصات: `tsc --noEmit` نظيف · `next build` نظيف · **531 اختباراً ناجحاً** (48 ملفاً).

---

## 1) المشكلات المكتشفة والمُصلَحة

### 1.1 عالية — `schema.sql` لم يواكب v25 (انحراف التثبيت الجديد)

- **الأثر:** أي **نشر جديد** كان سيحصل على قاعدة فيها الفهرس الفريد القديم
  `uq_profiles_one_additional_per_company` وبلا عمود `max_additional_users`
  ولا المشغّل ولا دالة الضبط. النتيجة: الميزة **مفقودة صامتاً**، وسلوك مختلف
  تماماً عن القواعد المُرحَّلة — وهو أسوأ من عطل ظاهر لأنه لا يُكتشف إلا متأخراً.
- **السبب:** ركّزت الترحيلة على القواعد القائمة ونسيت ملف التثبيت الجديد.
- **الإصلاح:** مزامنة `supabase/schema.sql` بالكامل: العمود + القيد + حذف
  الفهرس القديم + `ix_profiles_company_role` + `enforce_additional_user_limit`
  + `trg_profiles_additional_limit` + `admin_set_company_user_limit_v25`
  + تحديث `admin_get_company_extras_v18`.
- **الاختبار:** «اتساق schema.sql مع ترحيلة v25» (4 اختبارات).

### 1.2 عالية — إعادة تشغيل v11 كانت تُعيد القيد القديم

- **الأثر:** `migration_extra_features_users_v11.sql` ينشئ الفهرس الفريد بلا
  شرط. تشغيله على قاعدة مُرحَّلة (وارد جداً — الملفات موصوفة بأنها آمنة
  للتكرار) كان **يكسر تعدّد المستخدمين بصمت** ويعيد الحد إلى واحد.
- **الإصلاح:** الإنشاء صار مشروطاً داخل `do $$` — يُتخطّى إذا وُجد عمود
  `max_additional_users`، مع `raise notice` توضيحي.
- **الاختبار:** «توافق v11 القديمة مع v25».

### 1.3 متوسطة (وقائية) — `esc()` لا يرمّز الاقتباسات رغم استخدامه داخل خصائص HTML

- **الموضع:** `src/lib/exporter.ts` و`statementDoc.ts` و`dataExport.ts` كانت
  ترمّز `& < >` فقط، بينما القيم تُحقن في خصائص مثل
  `<img src="${esc(...)}" alt="${esc(...)}">`. نص يحتوي `"` يخرج من الخاصية
  ويحقن `onerror=`.
- **هل كانت مستغَلّة؟ لا.** المسار الوحيد الواصل لخاصية هو `logo_url` وهو
  محميّ بـ `safeUrl()` (بروتوكول https/http + `looksMalicious` + طول أقصى).
  لكنها **ثغرة على بُعد إعادة هيكلة واحدة**: أي حقل نصي جديد يوضع في خاصية
  يصبح ثغرة XSS فورية. `invoice-template-html.ts` كان يرمّزها أصلاً — تناقض
  بين الملفات.
- **الإصلاح:** توحيد الترميز ليشمل `"` و`'` في الثلاثة.
- **الاختبار:** «يعقّم الاقتباسات فلا يمكن الخروج من خاصية HTML».

### 1.4 منخفضة — سلوك هش في فحص تفرّد الهاتف + تسمية ناقصة

- `maybeSingle()` على `profiles.phone` كان يُرجع **500 غامضاً** لو وُجد أكثر
  من صف بنفس الهاتف (بيانات سابقة لحارس التفرّد) بدل رسالة 409 واضحة.
  أُبدل بـ `limit(1)` — الوجود وحده كافٍ للرفض.
- إجراء `admin.set_company_user_limit` الجديد لم يكن له اسم عربي في صفحة
  السجلّات، فكان يظهر كمفتاح خام. أُضيف.
- وصف الميزة في `features.ts` و`feature_catalog` كان ما زال يقول «حساب إضافي
  **واحد**». حُدِّث.

---

## 2) ما جرى فحصه ووُجد سليماً

### المحاسبة — لا أخطاء
- `round2` يستخدم `Number.EPSILON` فيعالج حالات النصف (`1.005 → 1.01`).
- الضريبة تُحسب على وعاء يشمل «المصروف الذي يتحمّله العميل» ولا تدخل الربح
  (تُعرض منفصلة في `vat_collected`) — سليم ضريبياً.
- المشتريات تُثبت **صافية** قبل ضريبة المدخلات (`purchaseNetFromRow`) فلا
  تُعامل الضريبة كمصروف P&L.
- السداد النقدي التلقائي لا يُحتسب مرتين (سندات الرحلات المتولّدة مستثناة
  عبر `source_expense_id is null`).
- إشعارات المدين/الدائن تدخل الرصيد والأرباح بالإشارة الصحيحة.
- معادلة ميزان المراجعة متحققة باختبار حقيقي على بيانات مرجعية.

### الأمن — لا ثغرات مستغَلّة
- **12 مسار API**: كلها تجمع `sameOrigin` (CSRF) + `rateLimit` + مصادقة.
  مسارات المطوّر تضيف `requireAdmin` + `verifyTwoFactorToken` (2FA).
- `serviceClient` (يتجاوز RLS) لا يُستدعى إلا **بعد** التحقق الكامل.
- كوكي 2FA: `httpOnly` + `SameSite=Strict` + بادئة `__Host-` في الإنتاج +
  مقارنة توقيع بـ `timingSafeEqual` (مقاومة توقيت) + `jti` لكل جلسة.
- عزل المستأجرين: كل السياسات تمر عبر `auth_company_id()`، والمستخدم الإضافي
  لا يحصل على `company_id` إلا إذا كان نشطاً والميزة مفعّلة.
- لا أسرار مُرمَّزة في المستودع، و`.env` مستثنى، و`anon key` وحده عام (صحيح).

### التعديلات نفسها — منطق سليم
- الحد مفروض **مرتين**: في الـ API (رسالة واضحة) وفي القاعدة (المشغّل مع قفل
  استشاري) — دفاع في العمق، فلا يمكن تجاوزه بطلب مباشر.
- `syncAdditionalFeature` يعكس الواقع (نشط واحد على الأقل) بدل الإطفاء الأعمى.
- الحذف يسبق المزامنة فلا ينقطع وصول البقية أثناء العملية.
- تراجع نظيف (`deleteUser`) عند فشل إدراج الملف الشخصي.

---

## 3) ملاحظات مفتوحة (ليست أعطالاً)

1. **لا صلاحيات مفصّلة.** كل مستخدم إضافي يرى بيانات الشركة كاملة بصلاحيات
   المالك. هذه أكبر فجوة وظيفية متبقية، وتحتاج طبقة صلاحيات عبر كل السياسات.
2. **`eslint` لا يعمل** — لا يوجد `eslint.config.js` رغم وجود السكربت
   والاعتماديات (مشكلة سابقة لهذا العمل). `tsc` يغطي الأخطاء النوعية.
3. **حد المستخدمين أقصاه 10** في القاعدة، والواجهة تعرض 0–5. رفعه يحتاج
   تعديل `USER_LIMIT_CHOICES` فقط.
