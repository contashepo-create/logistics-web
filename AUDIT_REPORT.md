# تقرير التدقيق الشامل — المحاسبي والبرمجي

تاريخ التدقيق: 2026-08-31 · النطاق: كامل مشروع `logistics-web` (Next.js + Supabase)
المرجع المعتمد للمطابقة المحاسبية: التطبيق المكتبي الأصلي `/home/user/logistic-src` (app/core/*.py)

---

## 1) خلاصة تنفيذية

- **النتيجة المحاسبية: مطابقة كاملة** لمحرك الحسابات الأصلي. تم التحقق باختبارات حقيقية تُقارن مخرجات نسخة الويب بأرقام التطبيق المكتبي (Oracle) سطراً بسطر.
- **عدد الاختبارات الحقيقية:** 51 اختباراً، كلها ناجحة، تغطي: الأرصدة، الفواتير، سندات القبض/الدفع، الرواتب وتسويات السلف، كشوف الحساب، التقارير الخمسة، معادلة ميزان المراجعة، الضريبة، الحفظ الذري وسباق الترقيم، وقواعد التحقق والتنسيق.
- **تم اكتشاف وإصلاح 6 أخطاء حقيقية** (أحدها كان سيكسر خصم السلف في الرواتب بالكامل عند الإنتاج).
- **تم سدّ ثغرة أمنية عالية الخطورة** في سياسة RLS لجدول `profiles` (إمكانية ربط حساب بشركة أخرى).
- **تم تنفيذ البنود المعمارية المفتوحة**: الحفظ الذري للفاتورة/الراتب عبر دوال RPC + قيود فريدة على أرقام المستندات + إعادة المحاولة عند تصادم الترقيم (انظر القسم 5).

---

## 2) الأخطاء المكتشفة والمُصلَحة

### 2.1 حرج — خصم السلف في الرواتب كان مكسوراً (`src/lib/repo.ts` → `savePayroll`)
- **السبب:** الواجهة (`dialogs/payroll.tsx`) ترسل `settlements` ككائنات `{ payment_voucher_id, amount }`، بينما `savePayroll` كانت تفكّكها كصفوف `[v, a]` (كما في النسخة المكتبية Python). النتيجة: عند أي راتب فيه خصم سلفة كان يحدث `TypeError` ولا يُحفظ الراتب.
- **الإصلاح:** قبول الصيغتين (صفوف وكائنات) بشكل دفاعي.
- **الاختبار:** `bugs.test.ts` → «يحفظ الراتب ويخصم السلفة…» + «يرفض خصم سلفة أكبر من المتبقي».

### 2.2 متوسط — مقارن ترتيب كشف حساب العميل غير سليم (`src/lib/calc.ts` → `customerStatement`)
- **السبب:** `rows.sort((a,b) => a.kind === "invoice" ? -1 : 1)` عند تساوي التاريخ — مقارن غير متعدٍّ/غير متماثل (يخالف عقد `sort`)، فيجعل ترتيب الحركات بنفس اليوم غير حتمي.
- **الإصلاح:** مقارن سليم (تاريخ ثم نوع المستند) مطابق للنسخة الأصلية.
- **الاختبار:** `bugs.test.ts` → «يرتّب الفاتورة قبل السند بنفس التاريخ…».

### 2.3 عالية — ثغرة RLS في `profiles` (عبور العزل بين الشركات)
- **السبب:** سياسة `profiles_own_insert ... with check (id = auth.uid())` كانت تسمح للمستخدم بتعيين `company_id` لأي قيمة عند إنشاء ملفه الشخصي، فيربط حسابه بشركة أخرى ويقرأ بياناتها (تُخفَّف المخاطرة فقط بصعوبة تخمين UUID).
- **الإصلاح:** `with check (id = auth.uid() and company_id is null)` — ربط الشركة يتم حصراً عبر `register_company` (security definer يتجاوز RLS).
- **الملفات:** `schema.sql`, `migration_company_id.sql`, `migration_subscription_2fa.sql` (قسم 5.1), `security_hardening.sql`.

### 2.4 متوسط — تقريب المبالغ يعاني انحراف الفاصلة العائمة (`src/lib/rules.ts` → `roundMoney` + `src/lib/calc.ts` → `round2`)
- **السبب:** `Math.round(x * 100) / 100` يُقرّب `1.005` إلى `1.00` بدل `1.01` لأن `1.005` يُمثَّل ثنائياً كـ `1.004999…`.
- **الإصلاح:** `Math.round((x + Number.EPSILON) * 100) / 100` — يعوّض الخطأ عند حدود «النصف» (x.xx5) فقط.
- **الاختبار:** `rules.test.ts` → «يعالج خطأ التمثيل الثنائي عند حالات النصف».

### 2.5 متوسط — نافذة تعديل الراتب تعرض «المتبقي» صفراً ولا تُتيح خصم سلف أخرى (`src/components/dialogs/payroll.tsx`)
- **السبب:** عند التعديل كانت السلف تُبنى من `p.settlements` فقط (بـ `remaining: 0`)، فلا تظهر السلف غير المخصومة في هذا الراتب ولا المتبقي الحقيقي.
- **الإصلاح:** تحميل السلف عبر `employeeAdvances` (نفس طريقة الإضافة) مع استرجاع خصومات هذا الراتب وإعادة جمعها في `remaining`.
- **الأثر:** واجهة التعديل أصبحت مطابقة للمنطق الخادمي (الذي كان يعيد المخصوم في `remMap` عند التعديل).

### 2.6 متوسط — نافذة «لقطة إغلاق السنة» كانت تعرض جداول فارغة دائماً (`src/components/dialogs/master.tsx` → `SnapshotDialog`)
- **السبب:** كانت تقرأ `snap?.data?.customers` بينما `getSnapshot` يعيد كائن اللقطة مباشرة (بلا تغليف في `.data`).
- **الإصلاح:** القراءة المباشرة `snap?.customers` / `snap?.cashboxes` / `snap?.banks` / `snap?.pnl`.

---

## 3) نتائج الاختبارات الحقيقية (قابلة لإعادة التشغيل)

التشغيل:
```bash
npm run test        # أو: npx vitest run
```

| الملف | عدد الاختبارات | النتيجة |
|---|---|---|
| `src/lib/__tests__/accounting.test.ts` | 11 | ✅ مطابقة كاملة لأرقام المرجع |
| `src/lib/__tests__/bugs.test.ts` | 4 | ✅ (تثبت الإصلاحات) |
| `src/lib/__tests__/rules.test.ts` | 14 | ✅ |
| `src/lib/__tests__/format.test.ts` | 15 | ✅ |
| `src/lib/__tests__/atomic.test.ts` | 7 | ✅ (الحفظ الذري + تصادم الترقيم + تحقق خادمي) |

### عيّنة من الأرقام المُتحقَّق منها (مطابقة لـ seed_demo المرجعي، بدون ضريبة):
- رصيد العميل 1: `17,500.00` · رصيد الخزينة: `16,130.00` · رصيد البنك: `79,900.00`
- إجماليات الفاتورة 1: نقلات `7,500` · مصروفات `1,130` · ربح متوقع `6,370` · مصاريف لاحقة `220` · ربح فعلي `6,150`
- أرباح وخسائر: إيرادات `10,050` · مصروفات `10,880` · صافي `-830`
- كشف الخزينة: افتتاحي `20,000` · ختامي `16,130` (6 حركات)
- معادلة ميزان المراجعة: `صافي الربح − Δالأصول = سندات الرحلات − المصروفات المباشرة` ✅

### اختبارات الضريبة (VAT 15%):
- فاتورة بنقلات `1,000` → ضريبة `150` → إجمالي العميل `1,150` ✅
- الضريبة **لا** تدخل في إيراد الربح (تُعرض في `vat_collected` منفصلة) ✅

---

## 4) مراجعة المحاسبة سطراً بسطر — الخلاصة

تمت مطابقة `calc.ts` مع `calc.py` و`repo.ts` مع `repo.py` و`rules.ts` مع `rules.py` و`format.ts` مع `fmt.py`. النتائج:

| الوظيفة | الحالة |
|---|---|
| `customerBalance` / `accountBalance` (الأرصدة لحظية من الحركات) | ✅ مطابق |
| `invoiceTotals` / `tripProfit` / `invoiceList` | ✅ مطابق |
| `customerStatement` / `accountStatement` | ✅ مطابق (بعد إصلاح الترتيب) |
| `employeeStatement` (رواتب/سلف/بدلات) | ✅ مطابق |
| `vehicleReport` / `tripProfitsReport` | ✅ مطابق |
| `pnlReport` (خمسة أقسام) | ✅ مطابق + إضافة `vat_collected` |
| `yearSnapshotData` | ✅ مطابق |
| قواعد التحقق (سنوات مالية/سقوف/أطوال/أنواع) | ✅ مطابق |
| حفظ/حذف كل الكيانات مع منع حذف المرتبط | ✅ مطابق |

### ملاحظة محاسبية عن نموذج السلف (صحيح وليس خطأ):
السلفة تُحسب كمصروف عند الصرف، والراتب يُسجَّل **بالصافي** بعد خصم السلفة — وبذلك إجمالي ما دفعه صاحب العمل = السلفة + الصافي، ولا يحدث ازدواج في الاحتساب. هذا هو النموذج المُتعمَّد في النظام البسيط (بدون دفتر أستاذ) وهو مطابق للمرجع.

### ملاحظة عن الضريبة:
الضريبة تُضاف إلى **رصيد العميل** (العميل مدين بالإجمالي شامل الضريبة) وتُستبعد من **إيراد الربح** — سلوك متّسق داخلياً وصحيح مبدئياً. لكن لا يوجد حساب "التزام ضريبي"، فمعادلة الميزان لا تتوازن كلياً عند ضريبة > 0 (الضريبة تبقى عائمة). إن أردت «ضريبة مرجعية بحتة» (لا تدخل في المديونية)، التعديل سطر واحد في `customerBalance` و`invoiceTotals.customer_total`. القرار لك — الوضع الحالي متّسق مع ما يظهر للعميل في الفاتورة.

---

## 5) بنود معمارية — **تم تنفيذها**

### 5.1 سباق أرقام المستندات + قيد فريد — **مُنفَّذ**
- أُضيف `unique (company_id, number)` على الجداول الأربعة (`invoices`, `receipt_vouchers`, `payment_vouchers`, `payrolls`) — ضمان قائم على قاعدة البيانات ضد تكرار الأرقام.
- السندات (`saveReceipt`/`savePayment`) تستخدم `insertNumbered`: تحسب `MAX+1` ثم تعيد المحاولة (حتى 5) عند رمز `23505`.

### 5.2 ذرّية حفظ الفاتورة والراتب — **مُنفَّذ**
- دالتا `save_invoice` و`save_payroll` (SQL، `SECURITY DEFINER`) تقفلان صف الشركة `FOR UPDATE` لتسلسل الترقيم لكل شركة، وتُنفِّذان الحفظ الكامل (الرأس + النقلات + المصروفات + سجل النشاط) في معاملة واحدة.
- `repo.ts` → `saveInvoice`/`savePayroll` تستدعيان هاتين الدالتين عبر `supabase.rpc(...)` (مع إبقاء التحقق المبكر الودّي).
- **الملفات:** `supabase/migration_atomic_saves.sql` (ترحيل قائم) + مضمّنة في `supabase/schema.sql` (تثبيت جديد).

### 5.3 تسوية السلف تُحذف وتُعاد كل مرة (مقبول)
- داخل `save_payroll` تُحذف `advance_settlements` المرتبطة ثم يُعاد إدراجها ضمن نفس المعاملة. سلوك مطابق للمرجع.

---

## 6) مراجعة الأمان

| البند | الحالة |
|---|---|
| عزل الشركات عبر RLS (`tenant_isolation`) على كل جداول البيانات | ✅ |
| حارس `set_company_id` يفرض `company_id` عند الإدراج | ✅ |
| منع تعديل `plan_type`/`is_active`/الاشتراك من قبل العميل (منح أعمدة فقط) | ✅ |
| دوال المطوّر تفحص `is_admin()` داخلياً (SECURITY DEFINER) | ✅ |
| 2FA للوحة المطوّر: OTP عبر تليجرام + كوكي موقّع HMAC (12 ساعة) + تقييد معدل | ✅ |
| OTP مخزّن كبصمة hash + حد 5 محاولات + مهلة 5 دقائق + تهدئة إعادة الإرسال | ✅ |
| مقارنة `timingSafeEqual` ضد هجمات التوقيت | ✅ |
| `notify-admin` يقرأ الطلب من قاعدة البيانات (لا يثق بمحتوى العميل) | ✅ |
| تصدير البيانات عبر `export_company_data` (SECURITY DEFINER، مقصور على شركة المستخدم) | ✅ |
| تهريب XSS في التصدير/الطباعة (`esc()` في exporter والفاتورة) | ✅ |
| أسرار تليجرام/2FA خادمية فقط (server-only + .env.local gitignored) | ✅ |
| تقييد معدل على نقاط 2FA والإشعار + تعقيم IP | ✅ |
| `profiles_own_insert` تمنع تعيين شركة أخرى | ✅ (أُصلحت أعلاه) |

### 6.1 متطلب نشر حرج (يجب التأكد منه):
`is_admin()` تعتمد على `auth.jwt() ->> 'email'`. **يجب تفعيل تأكيد البريد الإلكتروني في Supabase** (Authentication → Email → Confirm email). لو عُطِّل التأكيد، يستطيع أي شخص التسجيل بالبريد `conta.moha@gmail.com` ومن ثمّ بلوغ لوحة المطوّر واجتياز 2FA. هذا شرط لا غنى عنه.

### 6.2 ملاحظات أمان منخفضة الخطورة:
- دلو `receipts` عام ويسمح لأي مستخدم مُصادَق بالرفع إلى أي مسار داخله (لا عزل لكل مستخدم بالمسار). المقترح: مسارات `receipts/{user_id}/…` مع سياسة تقيّد ذلك.
- `admin_set_subscription` لا يحدّث `subscription_start` (بسيط وغير مؤثر).

---

## 7) ملاحظات منخفضة الخطورة (جودة / UX)

- الحقول المالية في الواجهة تستخدم `parseFloat` (وليس `parseFloatSafe`)، فإدخال «1,234.50» أو أرقام عربية يُفسَّر خطأ (يُقرأ «1»). يُنصح بتوحيد الاستخدام على `parseFloatSafe`.
- `saveInvoice`: لا يمكن تعيين ضريبة 0% لفاتورة واحدة عندما يكون الافتراضي 15% (الصفر يعيد الافتراضي).
- `saveYear` عند التعديل يتجاهل حقل الحالة (الحالة تُغيَّر عبر `setYearStatus` فقط) — حقل الحالة في نافذة التعديل مضلل.
- ~~التقريب `Math.round(x*100)/100` يعاني انحراف الفاصلة العائمة~~ — **أُصلح** (بند 2.4).
- المرفقات في الفاتورة تُخزَّن كأسماء ملفات فقط (لا رفع فعلي إلى Supabase Storage) — قيد متعمَّد من المرحلة الأولى، يُوثَّق هنا فقط.

---

## 8) خطوات أخيرة قبل النشر

1. **تطبيق ترحيلات SQL** على القاعدة القائمة:
   - `supabase/migration_subscription_2fa.sql` (إصلاح ثغرة `profiles` + الاشتراك/2FA).
   - `supabase/migration_atomic_saves.sql` (الحفظ الذري + القيود الفريدة على أرقام المستندات).
   - أو `schema.sql` لتثبيت جديد شامل.
2. **تفعيل تأكيد البريد** في Supabase (بند 6.1).
3. **إعادة توليد رمز بوت تليجرام** من @BotFather (الرمز الحالي ظهر في المحادثة).
4. **التحقق من وصول الخادم إلى `api.telegram.org`** في بيئة الإنتاج (فشل الوصول من بيئة التطوير فقط).
5. **تهيئة متغيّرات البيئة** في `.env.local` (عنوان Supabase + مفتاح anon + `ADMIN_2FA_SECRET` + رموز تليجرام). البناء يفشل بدون `NEXT_PUBLIC_SUPABASE_URL`.
6. تشغيل `npm run test` و`npx tsc --noEmit` و`npm run build` (الثلاثة خضراء حالياً: 51 اختباراً).
